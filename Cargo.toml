[package]
name = "vpt-rs"
version = "0.1.0"
edition = "2024"

[features]
default = []
windows-vss = []

[dependencies]
thiserror = "2"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "fmt"] }

[dev-dependencies]
tempfile = "3"
